package Strings;

public class metodosStrigs {
    public static void main(String[] args) {
        
       String str="";
       str.length="Longitud de la cadena";

       str.charAt(0)="Encontrar un caracter en una cadena mediante su indice";

       str.indexOf(str)="busca subcadenas y letras especificas";

       str.substring(0)="Busca subcadenas dentro de un string";

       str.toLowerCase()="convertir minusculas";

       str.toUpperCase()="convertir en MAYUSCULAS";

       str.toCharArray()="Convierte strings en arreglos de tipo char";

       str.concat(str)="Permite concatenar dos strings";

       str.trim()="Permite borrar espacios en blanco principio y final de las cadenas";


    }
    
}
